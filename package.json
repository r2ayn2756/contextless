{
  "name": "contextless",
  "version": "1.0.0",
  "description": "AI-powered context optimization tool - API and Web Interface",
  "main": "test-api.js",
  "scripts": {
    "test": "node test-api.js",
    "test-api": "node test-api.js",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../examples && npm install"
  },
  "keywords": [
    "ai",
    "text-compression",
    "api",
    "gemini",
    "context-optimization"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
